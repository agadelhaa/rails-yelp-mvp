<h2><%= @restaurant.name %></h2>
<p><%= @restaurant.address %></p>
<p><%= @restaurant.phone_number %></p>
<p><%= @restaurant.category %></p>

<ul class="list-group">
  <% @restaurant.reviews.each do |review| %>
    <li class="list-group-item"> Rating:<%= review.rating %> | <%= review.content %></li>
  <% end %>
</ul>

<%= simple_form_for [ @restaurant, @review ] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Submit review", class: "btn btn-success" %>
<% end %>

<%= link_to "Back", restaurants_path, class: "btn btn-outline-primary btn-sm"%>
<%# link_to "Leave a review", new_restaurant_review_path(@restaurant), class: "btn btn-outline-success btn-sm" %>
